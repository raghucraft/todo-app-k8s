pipeline {
    agent any
      
    stages {
        stage('checkout') {
            steps {
                checkout scm
            }
        }
        stage('Verifiy')
            steps {
                sh 'echo "Todo App pipeline started"'
                sh 'ls -la'
            }
    }